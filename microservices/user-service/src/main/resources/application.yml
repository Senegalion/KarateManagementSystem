spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      name: user-service+++
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
