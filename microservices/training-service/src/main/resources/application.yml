spring:
  application:
    name: training-service
  config:
    import: optional:configserver:http://localhost:8780
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
