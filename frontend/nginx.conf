events { worker_connections 1024; }

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  sendfile      on;
  tcp_nopush    on;
  tcp_nodelay   on;
  keepalive_timeout  65;

  gzip on;
  gzip_types text/plain text/css application/javascript application/json image/svg+xml;

  map $http_upgrade $connection_upgrade { default upgrade; '' close; }

  upstream api_gateway {
    server api-gateway:9191;
  }

  server {
    listen 80;
    server_name karate.local;
    return 301 https://karate.local$request_uri;
  }

  server {
    listen 443 ssl http2;
    server_name karate.local;

    ssl_certificate     /etc/nginx/certs/karate.local.crt;
    ssl_certificate_key /etc/nginx/certs/karate.local.key;

    ssl_protocols TLSv1.2 TLSv1.3;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
      proxy_pass         http://api-gateway:9191/;
      proxy_http_version 1.1;
      proxy_set_header   Host $host;
      proxy_set_header   X-Real-IP $remote_addr;
      proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header   X-Forwarded-Proto https;
    }

    location / {
      try_files $uri $uri/ /index.html;
    }

    location ~* \.(?:js|css|woff2?|ttf|eot|svg|png|jpg|jpeg|gif)$ {
      expires 7d;
      access_log off;
      try_files $uri =404;
    }

    error_page 404 /index.html;
  }
}
